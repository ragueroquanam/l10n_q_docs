<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_invoice_account_q_tree" model="ir.ui.view">
            <field name="name">account.invoice.list</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_invoice_tree"/>
            <field name="arch" type="xml">
                <field name="amount_residual_signed" position="after">
                    <field name="amount_residual" string="Cantidad a pagar en moneda" sum="Cantidad a pagar en moneda" optional="hide"/>
                </field>
            </field>
    </record>
    <record id="view_out_invoice_tree_inherit_block_button" model="ir.ui.view">
        <field name="name">account.move.out.invoice.tree.inherit.block.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list/header" position="inside">
                <button name="action_toggle_block_payment_multi"
                        string="Des(Bloquear Pago)"
                        type="object"
                        class="btn-secondary"/>
                <button name="action_open_multi_payment_wizard"
                        string="Pagar en múltiples formas"
                        type="object"/>
            </xpath>
        </field>
    </record>

    <record id="view_in_invoice_bill_tree" model="ir.ui.view">
        <field name="name">account.out.invoice.list.account.q</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_in_invoice_bill_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list/header" position="inside">
                <button name="action_open_multi_payment_wizard"
                        string="Pagar en múltiples formas"
                        type="object"/>
            </xpath>
        </field>
    </record>
</odoo>
