<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="purchase_order_form_inherit" model="ir.ui.view">
            <field name="name">purchase.order.form.inherit</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='currency_id']" position="after">
                    <field name="is_form_edition_disabled" invisible="1"/>
                </xpath>
                <xpath expr="//button[@name='button_cancel']" position="attributes">
                    <attribute name="confirm">¿Está seguro de que deseas cancelar esta orden?</attribute>
                </xpath>
            </field>
        </record>
        
        <record model="ir.ui.menu" id="purchase.purchase_report">
            <field name="groups_id" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        </record>
        <record model="ir.ui.menu" id="purchase.purchase_report_main">
            <field name="groups_id" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        </record>
    </data>
</odoo>